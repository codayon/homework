const SubHeader = ({ className, SubHeaderText }) => {
  return <h4 className={`${className} text-accent font-poppins font-medium tracking-widest text-md`}>{SubHeaderText}</h4>;
};

export default SubHeader;
